name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  fallow:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Fallow - Codebase Intelligence
        uses: fallow-rs/fallow@v2.86.0
